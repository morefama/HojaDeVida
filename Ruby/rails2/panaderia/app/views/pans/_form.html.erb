<%= form_with(model: pan, local: true) do |form| %>
  <% if pan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pan.errors.count, "error") %> prohibited this pan from being saved:</h2>

      <ul>
      <% pan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :descption %>
    <%= form.text_area :descption %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
